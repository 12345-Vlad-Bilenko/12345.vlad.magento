<?php
$collection = $block->getProductCollection();
$categoryId = $block->getCategoryId();
?>

<style>
    .product-widget-container {
        border: 2px solid #ccc; /* Бордер для всього блоку */
        padding: 20px;
        margin: 20px 0; /* Відступи зверху та знизу */
        background-color: #f9f9f9; /* Колір фону блоку */
    }

    .product-widget-container ul {
        list-style-type: none;
        padding: 0;
        display: flex;
        flex-wrap: wrap; /* Дозволяє переносити елементи */
        justify-content: space-between; /* Розподіл товарів по ширині */
    }

    .product-widget-container li {
        border: 1px solid #ddd; /* Бордер для кожного товару */
        border-radius: 5px; /* Закруглені кути */
        margin: 10px;
        padding: 15px;
        width: calc(20% - 20px); /* 5 товарів в ряд, враховуючи відступи */
        box-sizing: border-box; /* Включає відступи в ширину */
        transition: transform 0.3s ease, background-color 0.3s ease; /* Анімація */
    }

    .product-widget-container li:hover {
        transform: scale(1.05); /* Збільшення */
        background-color: #e0f7fa; /* Зміна кольору фону при наведенні */
    }

    .product-widget-container img {
        max-width: 100%; /* Ширина зображення */
        height: auto;
    }

    .product-widget-container p {
        margin: 5px 0;
    }

    .product-widget-container form {
        display: flex; /* Вирівнюємо елементи всередині форми */
        justify-content: center; /* Центруємо кнопку */
        margin-top: 10px; /* Відступ зверху */
    }

    .product-widget-container button {
        background-color: #28a745; /* Колір фону кнопки */
        color: white; /* Колір тексту */
        border: none; /* Без бордера */
        border-radius: 5px; /* Закруглені кути кнопки */
        padding: 10px 15px; /* Внутрішні відступи */
        cursor: pointer; /* Курсор при наведенні */
        transition: background-color 0.3s ease; /* Плавна зміна кольору */
    }

    .product-widget-container button:hover {
        background-color: #218838; /* Темніший зелений при наведенні */
    }
</style>


<p>Selected Category ID: <?php echo $categoryId; ?>!!!</p>

<div class="product-widget-container">
    <?php if (!empty($collection) && count($collection) > 0): ?>
        <ul>
            <?php foreach ($collection as $product): ?>
                <li>
                    <a href="<?php $prodhtml=$product->getProductUrl(); echo $product->getProductUrl(); ?>">
                        <?php echo $product->getName(); ?>
                    </a>

                    <?php if ($product->getImageUrl()): ?>
                        <img src="<?php $imghtml=$product->getImageUrl(); echo $product->getImageUrl(); ?>" alt="<?php echo $product->getName(); ?>">
                    <?php endif; ?>
                    <?php
// Виводимо інформацію про продукт і зображення, якщо вони існують
$productOutput = isset($producthtml) ? $producthtml : 'No product info';
$imageOutput = isset($imghtml) ? $imghtml : 'No image available';
?>

<p style="background-color: #28a745; color: white; padding: 10px; border-radius: 5px;">
    <?php echo $productOutput; ?> +++ <?php echo $imageOutput; ?>
</p>

                    <p><?php echo number_format($product->getPrice(), 2, '.', ' '); ?> $</p>

                    <?php if ($product->getRatingSummary()): ?>
                        <p>Rating: <?php echo $product->getRatingSummary()->getRatingSummary(); ?>%</p>
                    <?php endif; ?>

                    <?php if ($product->getTypeId() === 'configurable' && $product->getAvailableProductTypes()): ?>
                        <p>Available Sizes:</p>
                        <ul>
                            <?php foreach ($product->getAvailableProductTypes() as $size): ?>
                                <li><?php echo $size; ?></li>
                            <?php endforeach; ?>
                        </ul>
                    <?php endif; ?>

                    <form action="<?php echo $block->getUrl('checkout/cart/add'); ?>" method="post">
                        <input type="hidden" name="product" value="<?php echo $product->getId(); ?>">
                        <button type="submit">Add to Cart</button>
                    </form>
                </li>
            <?php endforeach; ?>
        </ul>
    <?php else: ?>
        <p>No products found in this category.</p>
    <?php endif; ?>
    
    



</div>

